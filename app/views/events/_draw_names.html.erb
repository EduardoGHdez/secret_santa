<%= form_with url: draw_names_event_path(event), method: :put,
  html: { class: "flex justify-between mx-auto w-full" } do |form| %>
  <%= form.submit "Draw Names", class: "shadow bg-purple-500 hover:bg-purple-600 focus:shadow-outline focus:outline-none text-white font-bold py-2 px-4 my-2 mx-auto w-full max-w-md" %>
<% end %>
